import { StripedRowsDirective } from './striped-rows.directive';

describe('StripedRowsDirective', () => {
  it('should create an instance', () => {
    const directive = new StripedRowsDirective();
    expect(directive).toBeTruthy();
  });
});
